# MyTime - CrossPlatform Time Tracking App

## Project Overview
A Monorepo for time tracking application built with Rust that precisely tracks users' foreground application usage. Designed for heavy users who need accurate time management data.

## Key Features
- Tracks foreground window usage exclusively
- Simple GUI with Start/Stop buttons and usage chart
- System tray integration for background operation
- CSV storage for time tracking data
- Portable Windows application

## Technical Stack
- **Language**: Rust
- **GUI Framework**: eframe/egui
- **Windows API**: windows-rs for foreground window tracking
- **Storage**: CSV files (no database in MVP)
- **Tray Icon**: tray-icon crate

## Build Instructions
Run these commands:
```
cargo build --release
```

Update memory often. If we see some errors, and fix it, we should update with errors and fix so we avoid repeating it.

Do not be smart and use "fallback"s or alternatives without my permission. You can iterate and use tools to try to solve, but do not just "let's simply" or "use fallback". If not possible, just tell me.